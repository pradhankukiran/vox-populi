# Local-dev pins for the vox-populi Modal backend.
# Production deps are baked into the Modal image (see app.py).
modal>=1.3.5
nano-vllm-voxcpm==2.0.1
huggingface_hub>=0.24.0
soundfile>=0.13.1
numpy>=1.26
fastapi[standard]>=0.115
python-multipart>=0.0.9
